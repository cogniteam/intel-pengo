name: docker-build:realsense2-updater

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build RealSense2 firmware updater Docker image
      run: docker build docker/realsense2-updater/ --tag intelpengo/realsense2-updater
