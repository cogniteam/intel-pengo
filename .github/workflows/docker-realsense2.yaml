name: docker-build:realsense2

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build Realsense Docker image
      run: docker build docker/realsense2/ --tag intelpengo/realsense2
